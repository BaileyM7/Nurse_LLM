line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = ["E501"]  # Black handles line length

[lint.per-file-ignores]
"tests/*" = ["B"]

[format]
quote-style = "double"
